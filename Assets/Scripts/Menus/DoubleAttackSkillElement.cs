using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Class for accessing the double attack skill element
public class DoubleAttackSkillElement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
