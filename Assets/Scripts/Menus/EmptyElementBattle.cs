using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Class for accessing the empty element battle element
public class EmptyElementBattle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
