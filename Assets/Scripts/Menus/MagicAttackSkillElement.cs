using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Class for accessing the magic attack skill element
public class MagicAttackSkillElement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
